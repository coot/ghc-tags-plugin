Haskell Library to work with CTAGS and ETAGS files
==================================================

* Create tags from *GHC* parsed representation.
* Parsing and formatting of `CTAGS` and `ETAGS` (`vim` and `emacs` style tags).
